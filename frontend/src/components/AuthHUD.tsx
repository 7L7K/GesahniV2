"use client";

import { useEffect, useState } from 'react';
import { useAuthState } from '@/hooks/useAuth';

export default function AuthHUD() {
    // Dev-only stub: no UI by default
    return null;
}
